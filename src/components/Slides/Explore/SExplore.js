import React from 'react';
import * as styles from "./styles.module.scss"

const SExplore = () => {
    return (

            <div className={styles.carousel_container}>
                <p className={styles.title}>EXPLORE</p>
                <p className={styles.subtitles}>Meet the Unisphere metaverse here. Find out what is how to apply and what to do in it.</p>
                <button className={styles.btnLearnMore}>More</button>
            </div>

    );
};

export default SExplore;

